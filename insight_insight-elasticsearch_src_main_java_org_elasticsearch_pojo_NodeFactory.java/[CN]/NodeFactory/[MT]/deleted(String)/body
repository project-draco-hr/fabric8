{
  Node node=nodes.remove(pid);
  if (node != null) {
    if (group != null) {
      group.leave(eid);
    }
    ServiceRegistration reg=this.services.remove(pid);
    if (reg != null) {
      reg.unregister();
    }
    node.close();
  }
}
