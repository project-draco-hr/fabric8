{
  int size=things.size();
  if (size > 0) {
    int value=counter.incrementAndGet();
    int index=value % size;
    if (index < 0) {
      index=0;
    }
 else     if (index >= size) {
      index=size - 1;
    }
    return things.get(index);
  }
  return null;
}
