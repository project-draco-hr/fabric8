{
  return Metadata.forCommand(CamelListComponentCommand.class).name("Camel: List Component").category(Categories.create(CATEGORY)).description("Lists all the components the current project includes");
}
