{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("file:target/inbox?delete=true").routeId("in-route").to("file:target/outbox").id("out");
    }
  }
;
}
