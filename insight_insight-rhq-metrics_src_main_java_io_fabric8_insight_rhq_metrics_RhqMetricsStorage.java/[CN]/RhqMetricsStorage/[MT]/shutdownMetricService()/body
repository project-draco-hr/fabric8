{
  if (metricsService != null) {
    try {
      LOG.info("Shutting down metrics service " + metricsService);
      metricsService.shutdown();
    }
  finally {
      metricsService=null;
    }
  }
}
