{
  long c=0;
  for (  int value : values) {
    c+=value;
  }
  double mean=(1.0 * c) / values.length;
  double rc=0;
  for (  int value : values) {
    double v=value - mean;
    rc+=(v * v);
  }
  return Math.sqrt(rc / values.length);
}
