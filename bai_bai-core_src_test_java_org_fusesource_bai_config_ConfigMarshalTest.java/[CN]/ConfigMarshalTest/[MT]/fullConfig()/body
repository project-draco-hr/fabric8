{
  PolicySet config=new PolicySet();
  config.addPolicy("full").excludeContext("*","audit*").includeEndpoint("activemq:*").excludeEvent(EventType.FAILURE_HANDLED).filter().xpath("/person[@name='James']");
  printXml(config);
}
