{
  AuditConfig config=new AuditConfig();
  config.addPolicy("myId").excludeContext("*","audit*").includeEndpoint("activemq:*").excludeEvent(EventType.FAILURE_HANDLED);
  String xml=ConfigHelper.toXml(config);
  System.out.println("XML: " + xml);
}
