{
  for (  String requirement : descriptor.getRequirements()) {
    if (getPatchProfile(version,requirement) == null) {
      throw new RuntimeException(String.format("Unable to install patch '%s' - required patch '%s' is missing in version %s",descriptor.getId(),requirement,version.getId()));
    }
  }
}
