{
  String basedir=System.getProperty("basedir",".");
  String path=basedir + "/src/test/resources";
  File dir=new File(path);
  assertTrue("Kube directory " + dir + " does not exist! Please supply the correct value in the "+ SYSTEM_PROPERTY_KUBE_DIR+ " system property value",dir.exists());
  return dir;
}
