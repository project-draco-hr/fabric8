{
  StringBuilder sb=new StringBuilder();
  boolean first=true;
  for (  Container container : containers) {
    if (first) {
      first=false;
    }
 else {
      sb.append(",");
    }
    sb.append(container.getId());
  }
  return sb.toString();
}
