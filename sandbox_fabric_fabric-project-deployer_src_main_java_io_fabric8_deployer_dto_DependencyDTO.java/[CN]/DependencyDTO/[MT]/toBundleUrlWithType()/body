{
  String answer=toBundleUrl();
  if (type != null && (!type.equals("jar") && !type.equals("bundle"))) {
    answer+="/" + type;
    if (classifier != null) {
      answer+="/" + classifier;
    }
  }
  return answer;
}
