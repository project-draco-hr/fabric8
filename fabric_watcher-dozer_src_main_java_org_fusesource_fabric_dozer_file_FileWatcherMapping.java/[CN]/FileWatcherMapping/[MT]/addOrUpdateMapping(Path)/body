{
  try {
    String url=toUrlString(path);
    Path old=urlMap.put(url,path);
    if (old == null) {
      addMapping(path);
    }
 else {
      updateMapping(path);
    }
  }
 catch (  Exception e) {
    LOG.warn("Ignored path " + path + " due to: "+ e,e);
  }
}
