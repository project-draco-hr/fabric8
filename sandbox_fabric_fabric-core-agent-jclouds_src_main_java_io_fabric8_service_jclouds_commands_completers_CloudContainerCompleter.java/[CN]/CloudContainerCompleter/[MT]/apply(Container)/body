{
  if (container != null && container.getMetadata() != null && container.getMetadata().getCreateOptions() != null && container.getMetadata().getCreateOptions().getProviderType().equals("jclouds")) {
    return true;
  }
 else   return false;
}
