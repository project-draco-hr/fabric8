{
  int h1=a.hashCode();
  int h2=b.hashCode();
  if (expected) {
    Assert.assertEquals("hashCode " + a + ", "+ b,h1,h2);
    Assert.assertEquals("" + a + ", "+ b,a,b);
  }
 else {
    Assert.assertTrue("" + a + " != "+ b,!a.equals(b));
  }
}
