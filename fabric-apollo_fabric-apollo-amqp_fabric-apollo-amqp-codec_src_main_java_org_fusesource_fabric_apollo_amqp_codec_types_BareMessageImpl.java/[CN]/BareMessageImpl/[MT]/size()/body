{
  long rc=0;
  if (properties != null) {
    rc+=properties.size();
  }
  if (applicationProperties != null) {
    rc+=properties.size();
  }
  if (data != null) {
    rc+=dataSize();
  }
  return rc;
}
