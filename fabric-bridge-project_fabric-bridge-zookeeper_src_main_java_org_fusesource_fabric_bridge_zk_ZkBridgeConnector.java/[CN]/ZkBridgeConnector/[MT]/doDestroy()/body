{
  try {
    super.doDestroy();
  }
 catch (  Exception e) {
    LOG.warn("Error destroying Bridge: " + e.getMessage(),e);
  }
  if (agent != null) {
    ZkConfigHelper.removeBridge(zooKeeper,agent);
  }
}
