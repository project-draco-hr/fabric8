{
  return new RouteBuilder(){
    public void configure(){
      from("file:src/test/data?noop=true").process(C24IOSource.c24Source(DocumentElement.class).xmlSource()).process(transform(Mx2MtTransform.class)).to("mock:result");
    }
  }
;
}
