{
  String[] values={"route1 AND blob OR AND","route1 AND (blob OR fred"};
  for (int i=0; i < values.length; i++) {
    try {
      evaluate(values[i],2);
      fail("Should have failed for " + values[i]);
    }
 catch (    RecognitionException e) {
    }
  }
}
