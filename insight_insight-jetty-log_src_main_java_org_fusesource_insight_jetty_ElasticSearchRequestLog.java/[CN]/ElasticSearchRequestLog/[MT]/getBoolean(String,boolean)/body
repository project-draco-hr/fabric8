{
  Object v=this.properties.get(name);
  if (v instanceof Boolean) {
    return (Boolean)v;
  }
 else   if (v == null) {
    return def;
  }
 else {
    return Boolean.parseBoolean(v.toString());
  }
}
