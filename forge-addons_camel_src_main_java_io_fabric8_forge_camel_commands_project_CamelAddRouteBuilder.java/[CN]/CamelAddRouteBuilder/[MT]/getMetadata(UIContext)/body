{
  return Metadata.forCommand(CamelAddRouteBuilder.class).name("project-camel-add-route-builder").category(Categories.create(CATEGORY)).description("Adds a Camel RouteBuilder class to your project");
}
