{
  if (startCounter.decrementAndGet() == 0) {
    running.set(false);
    try {
      group.close();
    }
 catch (    Throwable ignore) {
    }
    if (managedZkClient) {
      try {
        zkClient.close();
      }
 catch (      Throwable ignore) {
      }
      zkClient=null;
    }
  }
  if (tracker != null) {
    LOG.info("closing tracker");
    tracker.close();
  }
}
