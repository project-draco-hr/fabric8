{
  return gitReadOperation(new GitOperation<List<CommitInfo>>(){
    @Override public List<CommitInfo> call(    Git git,    GitContext context) throws Exception {
      return doHistory(git,objectId,pathOrBlobPath,limit);
    }
  }
);
}
