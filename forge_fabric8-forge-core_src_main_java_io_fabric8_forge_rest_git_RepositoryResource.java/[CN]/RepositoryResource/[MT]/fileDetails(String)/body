{
  return gitReadOperation(new GitOperation<Response>(){
    @Override public Response call(    Git git,    GitContext context) throws Exception {
      return doFileDetails(git,path);
    }
  }
);
}
