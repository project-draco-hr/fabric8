{
  return gitReadOperation(new GitOperation<CommitInfo>(){
    @Override public CommitInfo call(    Git git,    GitContext context) throws Exception {
      return doCommitInfo(git,commitId);
    }
  }
);
}
