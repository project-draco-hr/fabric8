{
  return Metadata.forCommand(getClass()).category(Categories.create(AbstractDevOpsCommand.CATEGORY)).name(AbstractDevOpsCommand.CATEGORY + ": Project New").description("Create a new project with DevOps configuration");
}
