{
  LOGGER.info("Destroying zookeeper server: {}",destroyable);
  if (registration != null) {
    registration.unregister();
    registration=null;
  }
  if (destroyable != null) {
    destroyable.destroy();
    destroyable=null;
  }
}
