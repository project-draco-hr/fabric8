{
  super(bindAddress,resolver,globalResolver,manualIp,minimumPort,maximumPort,profiles,version,dataStoreProperties,getZooKeeperServerPort,zooKeeperServerConnectionPort,DEFAULT_TICKTIME,DEFAULT_INIT_LIMIT,DEFAULT_SYNC_LIMIT,DEFAULT_DATA_DIR,zookeeperPassword,ensembleStart,agentEnabled,waitForProvision,provisionTimeout,DEFAULT_MIGRATION_TIMEOUT,autoImportEnabled,importPath,users,clean);
  this.name=name;
  this.parent=parent;
  this.providerType=providerType;
  this.ensembleServer=ensembleServer;
  this.preferredAddress=preferredAddress;
  this.number=number;
  this.proxyUri=proxyUri;
  this.zookeeperUrl=zookeeperUrl;
  this.jvmOpts=jvmOpts;
  this.adminAccess=adminAccess;
  this.systemProperties=systemProperties;
}
