{
  boolean retry=false;
  while (true) {
    try {
      if (!retry || super.exists(path) != null) {
        super.delete(path);
      }
      return;
    }
 catch (    KeeperException.ConnectionLossException ex) {
      retry=true;
    }
  }
}
