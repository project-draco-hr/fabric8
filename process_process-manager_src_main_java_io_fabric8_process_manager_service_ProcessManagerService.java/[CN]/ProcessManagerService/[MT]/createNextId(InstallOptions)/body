{
  String id=options.getId();
  if (Strings.isNotBlank(id)) {
    return id;
  }
  File dir;
  String answer=null;
  do {
    lastId++;
    answer="" + lastId;
    dir=createInstallDir(answer);
  }
 while (dir.exists());
  return answer;
}
