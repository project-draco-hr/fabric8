{
  return Metadata.forCommand(CamelAddComponentCommand.class).name("Camel: Add Component").category(Categories.create(CATEGORY)).description("Adds a Camel component to your project dependencies");
}
