{
  if (threadMaximumPoolSize > 0) {
    executorService=new ThreadPoolExecutor(1,threadMaximumPoolSize,60,TimeUnit.SECONDS,new SynchronousQueue<Runnable>(),new ThreadFactory("MavenDownloadProxyServlet"));
    executorService.allowCoreThreadTimeOut(true);
  }
  super.start();
}
