{
  KubernetesClient kube=new KubernetesClient();
  System.out.println("Connecting to kubernetes on: " + kube.getAddress());
  WebSocketClient client=kube.watchPods("jimmi",null,new ExampleWatcher());
  Thread.sleep(10000l);
  client.stop();
}
