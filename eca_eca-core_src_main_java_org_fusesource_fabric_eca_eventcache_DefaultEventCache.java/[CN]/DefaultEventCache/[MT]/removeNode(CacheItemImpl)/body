{
  if (node != null) {
    set.remove(node.item);
    if (root == node) {
      root=node.getNext();
    }
    if (tail == node) {
      tail=node.getPrevious();
    }
    node.unlink();
    size--;
  }
}
