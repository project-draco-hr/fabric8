{
  Logger LOG=LoggerFactory.getLogger(Log4jTest.class);
  LOG.info(info1);
  LOG.debug(debug1);
  LOG.warn(warn1);
  LOG.error(error1);
  LOG.info(info2);
  LOG.debug(debug2);
  LOG.warn(warn2);
  LOG.error(error2);
  assertLogQuery(logQuery);
}
