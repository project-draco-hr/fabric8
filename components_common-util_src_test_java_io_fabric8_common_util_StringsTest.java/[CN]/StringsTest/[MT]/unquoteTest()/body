{
  assertEquals("foobar",Strings.unquote("\"foobar\""));
  assertEquals("\"foobar\"",Strings.unquote("\"\"foobar\"\""));
  assertEquals("'foobar'",Strings.unquote("'foobar'"));
  Assert.assertNull(Strings.unquote(null));
}
