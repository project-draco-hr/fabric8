{
  return Metadata.forCommand(CamelAddRouteBuilder.class).name("project-camel-add-blueprint-xml").category(Categories.create(CATEGORY)).description("Adds a Blueprint XML file with CamelContext included to your project");
}
