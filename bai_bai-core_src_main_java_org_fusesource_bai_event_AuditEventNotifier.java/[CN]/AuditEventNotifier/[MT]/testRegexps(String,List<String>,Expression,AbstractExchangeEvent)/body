{
  if (endpointURI == null) {
    return testFilter(filter,exchangeEvent);
  }
  for (  String regex : regexps) {
    if (endpointURI.matches(regex)) {
      return testFilter(filter,exchangeEvent);
    }
  }
  return false;
}
