{
  return new CreateSshContainerOptions(bindAddress,resolver,globalResolver,manualIp,minimumPort,maximumPort,profiles,zooKeeperServerPort,zooKeeperServerConnectionPort,zookeeperPassword,agentEnabled,autoImportEnabled,importPath,users,name,parent,"ssh",ensembleServer,preferredAddress,systemProperties,number,proxyUri,zookeeperUrl,jvmOpts,adminAccess,version,username,password,host,port,sshRetries,retryDelay,privateKeyFile,passPhrase,path,environmentalVariables);
}
