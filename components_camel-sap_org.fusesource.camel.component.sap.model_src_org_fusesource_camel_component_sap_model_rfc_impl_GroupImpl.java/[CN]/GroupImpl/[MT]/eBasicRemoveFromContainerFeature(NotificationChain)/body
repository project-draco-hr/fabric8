{
switch (eContainerFeatureID()) {
case RfcPackage.GROUP__REPOSITORY:
    return eInternalContainer().eInverseRemove(this,RfcPackage.REPOSITORY__GROUPS,Repository.class,msgs);
}
return super.eBasicRemoveFromContainerFeature(msgs);
}
