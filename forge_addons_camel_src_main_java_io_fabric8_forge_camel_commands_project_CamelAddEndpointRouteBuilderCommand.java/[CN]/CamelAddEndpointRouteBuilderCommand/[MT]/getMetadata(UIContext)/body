{
  return Metadata.forCommand(CamelAddEndpointRouteBuilderCommand.class).name("Camel: New Endpoint RouteBuilder").category(Categories.create(CATEGORY)).description("Adds a Camel endpoint to an existing RouteBuilder");
}
