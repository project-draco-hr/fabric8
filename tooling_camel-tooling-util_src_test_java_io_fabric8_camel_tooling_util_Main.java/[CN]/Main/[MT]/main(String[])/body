{
  if (args.length == 0) {
    System.out.println("Usage: camelXmlFile");
  }
 else {
    RouteXml helper=new RouteXml();
    XmlModel model=helper.unmarshal(new File(args[0]));
    System.out.println("Found routes: " + model.getRouteDefinitionList());
  }
}
