{
  return Metadata.forCommand(CamelAddRouteBuilder.class).name("project-camel-add-spring-xml").category(Categories.create(CATEGORY)).description("Adds a Spring XML file with CamelContext included to your project");
}
