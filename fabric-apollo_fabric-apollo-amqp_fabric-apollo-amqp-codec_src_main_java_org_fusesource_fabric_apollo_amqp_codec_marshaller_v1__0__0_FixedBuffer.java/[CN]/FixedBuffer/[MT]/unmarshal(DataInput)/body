{
  Buffer rc=null;
  if (category.WIDTH > 0) {
    rc=new Buffer(1 + category.WIDTH);
    in.readFully(rc.data,1,category.WIDTH);
  }
 else {
    rc=new Buffer(1);
  }
  rc.data[0]=formatCode;
  return rc;
}
