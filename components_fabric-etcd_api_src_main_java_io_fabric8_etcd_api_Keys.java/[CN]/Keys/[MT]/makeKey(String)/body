{
  if (key == null) {
    throw new IllegalArgumentException("Key is null");
  }
 else   if (key.startsWith("/")) {
    return KEYS_PREFIX + key.substring(1);
  }
 else {
    return KEYS_PREFIX + key;
  }
}
