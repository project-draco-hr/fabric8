{
  try {
    zooKeeper.delete(ZkPath.WEBAPPS_CONTAINER.getPath(webEvent.getBundle().getSymbolicName(),webEvent.getBundle().getVersion().toString(),container.getId()));
  }
 catch (  Exception e) {
    LOGGER.error("Failed to unregister webapp {}.",webEvent.getContextPath(),e);
  }
}
