{
  String block="                                        ";
  int spaces=size;
  while (spaces > 0) {
    int n=Math.min(spaces,block.length());
    buffer.append(block.substring(0,n));
    spaces-=n;
  }
}
