{
  return Metadata.forCommand(CamelNewRouteBuilderCommand.class).name("Camel: New XML spring").category(Categories.create(CATEGORY)).description("Creates a new Spring XML file with CamelContext to your project");
}
