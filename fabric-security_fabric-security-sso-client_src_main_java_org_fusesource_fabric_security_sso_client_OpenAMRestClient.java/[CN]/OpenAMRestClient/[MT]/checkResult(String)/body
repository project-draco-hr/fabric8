{
  result=result.trim();
  if (!result.startsWith("boolean=")) {
    return false;
  }
  try {
    return Boolean.parseBoolean(result.split("=")[1]);
  }
 catch (  Exception e) {
    return false;
  }
}
