{
  List<Runnable> copyOfCallbacks=new ArrayList<Runnable>(callbacks);
  callbacks.clear();
  for (  Runnable callback : copyOfCallbacks) {
    try {
      callback.run();
    }
 catch (    Throwable t) {
    }
  }
}
