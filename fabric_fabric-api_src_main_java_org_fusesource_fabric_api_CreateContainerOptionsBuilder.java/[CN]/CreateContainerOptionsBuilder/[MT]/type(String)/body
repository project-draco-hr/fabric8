{
  if ("child".equals(type)) {
    return child();
  }
 else   if ("ssh".equals(type)) {
    return ssh();
  }
 else   if ("jclouds".equals(type)) {
    return jclouds();
  }
 else {
    return basic();
  }
}
