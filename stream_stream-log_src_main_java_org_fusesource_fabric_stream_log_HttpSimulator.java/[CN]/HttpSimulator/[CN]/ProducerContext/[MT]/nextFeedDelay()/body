{
  if (zeroDelays > 0) {
    zeroDelays-=1;
    if (zeroDelays == 0) {
      return 1;
    }
 else {
      return 0;
    }
  }
  if (feedingsPerMs > 1) {
    zeroDelays=(long)feedingsPerMs;
    return nextFeedDelay();
  }
 else {
    return (long)msPerFeedings;
  }
}
