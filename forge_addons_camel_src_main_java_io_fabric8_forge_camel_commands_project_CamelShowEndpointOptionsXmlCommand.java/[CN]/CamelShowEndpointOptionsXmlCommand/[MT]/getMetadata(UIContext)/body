{
  return Metadata.forCommand(CamelShowEndpointOptionsXmlCommand.class).name("Camel: Show options Endpoint XML").category(Categories.create(CATEGORY)).description("Show Camel endpoint options from an existing XML file");
}
