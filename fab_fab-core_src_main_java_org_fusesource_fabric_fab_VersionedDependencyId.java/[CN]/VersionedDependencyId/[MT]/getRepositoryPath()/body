{
  return getGroupId().replace('.','/') + "/" + getArtifactId()+ "/"+ getVersion()+ "/"+ getArtifactId()+ "-"+ getVersion()+ (notEmpty(getClassifier()) ? "-" + getClassifier() : "")+ "."+ getExtension();
}
