{
  int size=things.size();
  if (size == 1) {
    return things.get(0);
  }
 else   if (size > 1) {
    int idx=(int)Math.round(Math.random() * (size - 1));
    if (idx < 0) {
      idx=0;
    }
    if (idx >= size) {
      idx=size - 1;
    }
    return things.get(idx);
  }
  return null;
}
