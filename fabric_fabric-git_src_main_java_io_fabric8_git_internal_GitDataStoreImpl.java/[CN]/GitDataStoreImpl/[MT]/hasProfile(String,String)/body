{
  LockHandle readLock=aquireReadLock();
  try {
    assertValid();
    Profile profile=getProfileFromCache(versionId,profileId);
    return profile != null;
  }
  finally {
    readLock.unlock();
  }
}
