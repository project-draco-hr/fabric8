{
  return new RouteBuilder(){
    public void configure(){
      from("direct:createFlcustList").to("sap:destination:nplDest:BAPI_FLCUST_CREATEFROMDATA?transacted=true").to("mock:result");
    }
  }
;
}
