{
  Number prevNumber=(Number)prev.get(attribute);
  Number nextNumber=(Number)next.get(attribute);
  if (prevNumber == null || nextNumber == null)   return 0;
  return nextNumber.longValue() - prevNumber.longValue();
}
