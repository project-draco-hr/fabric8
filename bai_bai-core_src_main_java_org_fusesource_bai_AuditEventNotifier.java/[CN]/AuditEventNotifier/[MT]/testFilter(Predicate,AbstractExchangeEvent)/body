{
  if (filter == null) {
    return true;
  }
 else {
    Exchange exchange=exchangeEvent.getExchange();
    if (exchange != null) {
      return filter.matches(exchange);
    }
  }
  return false;
}
