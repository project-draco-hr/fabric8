{
  super(msg + "; Unable to create container for " + Arrays.toString(profiles.toArray())+ ", missing dependant container matching profiles: "+ Arrays.toString(dependencies)+ " or tags: "+ Arrays.toString(tags),cause);
}
