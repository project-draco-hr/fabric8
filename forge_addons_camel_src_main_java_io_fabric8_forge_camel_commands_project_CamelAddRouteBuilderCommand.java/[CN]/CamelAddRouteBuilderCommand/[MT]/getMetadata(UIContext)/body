{
  return Metadata.forCommand(CamelAddRouteBuilderCommand.class).name("Camel: New RouteBuilder").category(Categories.create(CATEGORY)).description("Adds a Camel RouteBuilder class to your project");
}
