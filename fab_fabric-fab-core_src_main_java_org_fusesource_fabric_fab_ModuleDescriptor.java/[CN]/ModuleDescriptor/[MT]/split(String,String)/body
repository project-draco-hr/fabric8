{
  ArrayList<String> rc=new ArrayList<String>();
  if (extensions != null) {
    for (    String v : extensions.split(sep)) {
      String trim=v.trim();
      if (trim.length() > 0) {
        rc.add(trim);
      }
    }
  }
  return rc;
}
