{
  String answer=null;
  try {
    if (true) {
      answer=Manifests.getManfiestEntry(connection.getJarFile(),name);
    }
 else {
      answer=instructions.getProperty(name,"");
    }
  }
 catch (  IOException e) {
  }
  if (answer == null) {
    answer="";
  }
  return answer;
}
