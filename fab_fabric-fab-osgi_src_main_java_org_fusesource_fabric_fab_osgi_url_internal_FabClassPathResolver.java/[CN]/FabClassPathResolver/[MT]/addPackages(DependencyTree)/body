{
  try {
    for (    String p : tree.getPackages()) {
      if (!dependenciesByPackage.containsKey(p)) {
        dependenciesByPackage.put(p,tree);
      }
    }
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
}
