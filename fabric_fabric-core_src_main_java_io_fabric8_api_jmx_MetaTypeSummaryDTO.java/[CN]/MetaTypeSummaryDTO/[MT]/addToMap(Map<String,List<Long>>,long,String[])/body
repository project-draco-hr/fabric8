{
  if (pids != null) {
    for (    String pid : pids) {
      List<Long> list=map.get(pid);
      if (list == null) {
        list=new ArrayList<Long>();
        map.put(pid,list);
      }
      list.add(bundleId);
    }
  }
}
