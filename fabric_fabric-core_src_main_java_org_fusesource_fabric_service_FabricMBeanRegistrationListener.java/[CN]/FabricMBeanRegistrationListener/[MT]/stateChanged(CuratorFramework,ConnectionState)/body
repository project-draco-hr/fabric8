{
  if (isValid()) {
switch (newState) {
case CONNECTED:
case RECONNECTED:
      executor.submit(new Runnable(){
        public void run(){
          updateProcessId();
        }
      }
);
    break;
}
}
}
