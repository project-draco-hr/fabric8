{
switch (newState) {
case CONNECTED:
case RECONNECTED:
    connected.set(true);
  if (isValid()) {
    register(MavenProxy.DOWNLOAD_TYPE);
    register(MavenProxy.UPLOAD_TYPE);
  }
break;
default :
connected.set(false);
break;
}
}
