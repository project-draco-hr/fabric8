{
  String status=container.getProvisionStatus();
  if (container.isRoot())   status=ZkDefs.SUCCESS;
  if (status == null) {
    return "";
  }
  status=status.trim();
  if (status.length() > 100) {
    return status.substring(0,100);
  }
 else {
    return status;
  }
}
