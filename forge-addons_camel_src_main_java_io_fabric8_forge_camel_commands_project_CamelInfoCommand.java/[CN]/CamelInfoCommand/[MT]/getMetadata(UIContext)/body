{
  return Metadata.forCommand(CamelInfoCommand.class).name("project-camel-info").category(Categories.create(CATEGORY)).description("Displays what the current project includes of Camel components");
}
