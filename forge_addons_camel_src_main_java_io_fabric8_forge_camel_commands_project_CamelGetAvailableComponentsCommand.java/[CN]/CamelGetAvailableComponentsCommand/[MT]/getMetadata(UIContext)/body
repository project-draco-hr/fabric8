{
  return Metadata.forCommand(CamelGetAvailableComponentsCommand.class).name("Camel: Get Available Components").category(Categories.create(CATEGORY)).description("Gets the available components that could be added to the current project");
}
