{
  LinkedList<String> result=new LinkedList<String>();
  for (  Container c : this.fabricService.getContainers()) {
    String name=c.getId();
    if (this.matches(pattern,name))     result.add(name);
  }
  return result;
}
