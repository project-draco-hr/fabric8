{
  List<String> answer=new ArrayList<String>();
  if (profiles != null) {
    for (    Profile profile : profiles) {
      String id=profile.getId();
      if (id != null) {
        answer.add(id);
      }
    }
  }
  return answer;
}
