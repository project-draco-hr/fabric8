{
  return new RouteBuilder(){
    public void configure(){
      Namespaces ns=new Namespaces("foo","http://www.c24.biz/testTransactions");
      from("file:src/test/data?noop=true").unmarshal().c24io(Transactions.class).filter(ns.xquery("//foo:Currency = 'GBP'")).to("mock:result");
    }
  }
;
}
