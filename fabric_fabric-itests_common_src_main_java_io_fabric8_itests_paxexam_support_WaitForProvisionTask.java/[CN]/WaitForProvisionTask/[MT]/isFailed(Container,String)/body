{
  try {
    return container.getProvisionException() != null || container.getProvisionException() != null || Container.PROVISION_FAILED.equals(container.getProvisionStatus()) || (container.getProvisionStatus() != null && container.getProvisionStatus().contains(Container.PROVISION_ERROR));
  }
 catch (  Throwable t) {
    return false;
  }
}
