{
  BufferedReader reader=new BufferedReader(new InputStreamReader(inputStream));
  try {
    while (true) {
      String line=reader.readLine();
      if (line == null)       break;
      LOGGER.debug("Error " + prefix + message+ ": "+ line);
    }
  }
 catch (  Exception e) {
    LOGGER.error("Failed to process " + prefix + message+ ": "+ e,e);
    throw e;
  }
 finally {
    Closeables.closeQuitely(reader);
  }
}
