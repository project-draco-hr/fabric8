{
  config=new HttpMappingRuleBase("/fabric/registry/clusters",new SimplePathTemplate(uriTemplate),version,enabledVersion,loadBalancer,reverseHeaders);
  gateway.addMappingRuleConfiguration(config);
}
