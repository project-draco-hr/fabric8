{
  return new RouteBuilder(){
    public void configure(){
      from("file:src/test/data?noop=true").unmarshal(dataFormat).filter().simple("${body.author[0].name} == 'Knuth'").to("mock:result");
    }
  }
;
}
