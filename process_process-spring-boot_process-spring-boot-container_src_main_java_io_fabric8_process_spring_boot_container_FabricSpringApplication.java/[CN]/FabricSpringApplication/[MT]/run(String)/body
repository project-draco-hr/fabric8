{
  SpringApplicationBuilder applicationBuilder=new SpringApplicationBuilder().sources(FabricSpringApplicationConfiguration.class);
  resolveWebEnvironment(applicationBuilder);
  return applicationBuilder.run(args);
}
