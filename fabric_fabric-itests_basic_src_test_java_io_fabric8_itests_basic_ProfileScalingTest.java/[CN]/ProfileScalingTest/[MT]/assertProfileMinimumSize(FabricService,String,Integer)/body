{
  boolean done=false;
  int tries=0;
  while (!done) {
    try {
      doAssertProfileMinimumSize(fabricService,profile,expected);
      done=true;
    }
 catch (    Error e) {
      if (++tries > 3) {
        throw e;
      }
    }
  }
}
