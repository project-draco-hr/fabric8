{
  if (bs.readBoolean()) {
    if (bs.readBoolean()) {
      return dataIn.readLong();
    }
 else {
      return toLong(dataIn.readInt());
    }
  }
 else {
    if (bs.readBoolean()) {
      return toLong(dataIn.readShort());
    }
 else {
      return 0;
    }
  }
}
