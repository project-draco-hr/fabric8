{
  if (baseUri != null) {
    String prefix=baseUri.toString();
    if (!prefix.endsWith("/")) {
      prefix+="/";
    }
    while (path.startsWith("/")) {
      path=path.substring(1);
    }
    return prefix + path;
  }
 else {
    return path;
  }
}
