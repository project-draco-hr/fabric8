{
  this.group=group;
  group.add(new GroupListener<CxfNodeState>(){
    @Override public void groupEvent(    Group<CxfNodeState> group,    GroupEvent event){
      onUpdate(group);
    }
  }
);
  if (group instanceof ZooKeeperGroup) {
    ((ZooKeeperGroup)group).clearAndRefresh(true,true);
  }
}
