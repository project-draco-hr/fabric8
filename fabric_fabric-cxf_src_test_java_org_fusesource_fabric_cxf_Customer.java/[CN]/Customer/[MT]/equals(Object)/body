{
  if (!(obj instanceof org.fusesource.fabric.cxf.Customer)) {
    return false;
  }
  if (this == obj) {
    return true;
  }
  Customer other=(Customer)obj;
  return id == other.id && name.equals(other.name);
}
