{
  if (data == null) {
    return null;
  }
 else {
    try {
      return data.getBytes(CHARSET);
    }
 catch (    UnsupportedEncodingException e) {
      throw new RuntimeException(e);
    }
  }
}
