{
  if (LOG.isDebugEnabled()) {
    LOG.debug("Sending batch containing " + messages.size() + " messages");
  }
  doSend(messages,sourceSession);
  if (LOG.isDebugEnabled()) {
    LOG.debug("Send complete for batch containing " + messages.size() + " messages");
  }
}
