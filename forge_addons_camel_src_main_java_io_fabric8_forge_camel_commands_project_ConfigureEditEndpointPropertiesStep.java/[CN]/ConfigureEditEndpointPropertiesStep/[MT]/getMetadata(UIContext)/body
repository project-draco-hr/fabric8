{
  return Metadata.forCommand(ConfigureEditEndpointPropertiesStep.class).name("Camel: Edit Endpoint XML").category(Categories.create(CATEGORY)).description("Configure the endpoint options to use");
}
