{
  return Metadata.forCommand(CamelEditEndpointRouteBuilderCommand.class).name("Camel: Edit Endpoint RouteBuilder").category(Categories.create(CATEGORY)).description("Edit Camel endpoint from an existing RouteBuilder");
}
