{
  try {
    destroy();
    reference=ref;
    manager=new Manager(this.bundleContext,(IZKClient)this.bundleContext.getService(reference),uri);
    manager.init();
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
}
