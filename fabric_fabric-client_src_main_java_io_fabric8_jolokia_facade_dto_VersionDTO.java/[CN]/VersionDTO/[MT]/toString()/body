{
  return String.format("Version: { id: %s, name: %s, version: %s, derivedFrom: %s, profiles: %s }",id,name,(sequence != null ? sequence.name : ""),(derivedFrom != null ? derivedFrom.id : ""),profiles);
}
