{
  return Metadata.forCommand(CamelAddRouteBuilder.class).name("Camel: New XML spring").category(Categories.create(CATEGORY)).description("Adds a new Spring XML file with CamelContext to your project");
}
