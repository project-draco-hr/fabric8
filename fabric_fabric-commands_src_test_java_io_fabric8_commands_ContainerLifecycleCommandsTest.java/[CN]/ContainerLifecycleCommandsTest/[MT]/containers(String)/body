{
  try {
    ReflectionHelper.setField(AbstractContainerLifecycleAction.class.getDeclaredField("containers"),this.stop,Arrays.asList(names));
  }
 catch (  Exception e) {
    throw new RuntimeException(e.getMessage(),e);
  }
}
