{
  for (  Object key : value.keySet()) {
    ((AmqpType)key).write(out);
    Object v=value.get(key);
    if (v == null) {
      writeNull(out);
    }
 else {
      ((AmqpType)v).write(out);
    }
  }
}
