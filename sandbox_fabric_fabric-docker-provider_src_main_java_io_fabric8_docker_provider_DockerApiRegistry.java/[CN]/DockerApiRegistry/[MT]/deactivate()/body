{
  try {
    apiCache.close();
  }
 catch (  IOException e) {
    LOG.warn("Failed to close apiCache " + e,e);
  }
  deactivateComponent();
  try {
    unregisterMBean();
  }
 catch (  Exception e) {
    LOG.warn("Caught while unregistering mbean on deactivate: " + e,e);
  }
}
