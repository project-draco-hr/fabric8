{
  return Metadata.forCommand(CamelAddComponentInstanceSpringCommand.class).name("Camel: New Component Spring").category(Categories.create(CATEGORY)).description("Adds a Camel component instance configuration using Spring to your project");
}
