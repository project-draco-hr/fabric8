{
  m_indices=(caseSensitive) ? new TreeMap<String,Map<Object,Set<C>>>() : new TreeMap<String,Map<Object,Set<C>>>(String.CASE_INSENSITIVE_ORDER);
  for (int i=0; (indexProps != null) && (i < indexProps.size()); i++) {
    m_indices.put(indexProps.get(i),new HashMap<Object,Set<C>>());
  }
}
