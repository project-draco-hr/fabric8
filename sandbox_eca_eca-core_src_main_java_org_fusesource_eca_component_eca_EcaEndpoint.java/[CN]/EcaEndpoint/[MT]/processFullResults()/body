{
  Object result=null;
  List<CacheItem<Exchange>> list=expression.getMatching();
  if (isRawResults()) {
    if (list != null && !list.isEmpty()) {
      result=list;
    }
  }
 else {
    result=processList(list);
  }
  return result;
}
