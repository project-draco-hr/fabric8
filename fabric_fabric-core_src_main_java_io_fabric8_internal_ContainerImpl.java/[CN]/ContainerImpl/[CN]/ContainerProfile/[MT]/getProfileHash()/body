{
  StringBuilder sb=new StringBuilder();
  boolean first=true;
  for (  Profile p : getProfiles()) {
    if (!first) {
      sb.append("-");
    }
 else {
      first=false;
    }
    sb.append(p.getProfileHash());
  }
  return sb.toString();
}
