{
  if (value instanceof Boolean || value instanceof Number) {
    buffer.append(value.toString());
  }
 else {
    buffer.append('"');
    buffer.append(value.toString());
    buffer.append('"');
  }
}
