{
  if (root.valid) {
    return true;
  }
  Node node=root;
  for (  Path p : path) {
    node=node.children.get(p.toString());
    if (node == null) {
      return false;
    }
 else     if (node.valid) {
      return true;
    }
  }
  return true;
}
