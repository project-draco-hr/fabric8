{
  Node node=root;
  for (  String path : groupId.split("\\.")) {
    Node child=node.children.get(path);
    if (child == null) {
      child=new Node();
      node.children.put(path,child);
    }
    node=child;
  }
  node.valid=true;
}
