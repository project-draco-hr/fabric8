{
  String answer="";
  String[] lines=text.split("\r?\n");
  for (  String line : lines) {
    String l=line.trim();
    if (!l.startsWith("##")) {
      answer=answer.concat(line);
      answer=answer.concat("\n");
    }
  }
  return answer;
}
