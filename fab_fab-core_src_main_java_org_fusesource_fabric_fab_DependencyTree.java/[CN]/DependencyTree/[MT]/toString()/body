{
  String classifier=getClassifier();
  String extension=getExtension();
  return "DependencyTree(" + getGroupId() + ":"+ getArtifactId()+ ":"+ version+ ":"+ (notEmpty(classifier) ? ":" + classifier : "")+ (notEmpty(extension) ? ":" + extension : "")+ children+ ")";
}
