{
  executor.submit(new Runnable(){
    public void run(){
      try {
        doUpdate(props);
      }
 catch (      Exception e) {
        LOGGER.error("Unable to update agent",e);
      }
    }
  }
);
}
