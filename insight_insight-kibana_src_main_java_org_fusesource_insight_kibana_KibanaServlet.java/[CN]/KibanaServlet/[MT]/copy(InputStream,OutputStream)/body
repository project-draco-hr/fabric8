{
  try {
    byte[] buffer=new byte[4096];
    int l;
    while ((l=inputStream.read(buffer)) >= 0) {
      outputStream.write(buffer,0,l);
    }
  }
  finally {
    inputStream.close();
    outputStream.close();
  }
}
