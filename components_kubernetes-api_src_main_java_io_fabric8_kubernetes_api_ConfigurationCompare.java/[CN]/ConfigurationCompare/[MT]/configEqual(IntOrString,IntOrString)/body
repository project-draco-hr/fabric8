{
  if (entity1 == entity2) {
    return true;
  }
 else   if (entity1 == null || entity2 == null) {
    return false;
  }
  return configEqual(entity1.getKind(),entity2.getKind()) && configEqual(entity1.getIntVal(),entity2.getIntVal()) && configEqual(entity1.getStrVal(),entity2.getStrVal());
}
