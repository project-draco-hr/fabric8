{
  if (entity == null) {
    return null;
  }
  if (clazz.isInstance(entity)) {
    return clazz.cast(entity);
  }
 else {
    throw new IllegalArgumentException("Invalid entity should be of type: " + clazz.getName() + " but was "+ entity.getClass().getName());
  }
}
