{
  if (entity1 == entity2) {
    return true;
  }
 else   if (entity1 == null || entity2 == null) {
    return false;
  }
  return configEqual(entity1.getName(),entity2.getName()) && configEqual(entity1.getValue(),entity2.getValue());
}
