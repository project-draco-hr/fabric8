{
  if (entity1 == entity2) {
    return true;
  }
 else   if (entity1 == null || entity2 == null) {
    return false;
  }
  return configEqual(entity1.getContainers(),entity2.getContainers()) || configEqual(entity1.getHostNetwork(),entity2.getHostNetwork()) || configEqual(entity1.getRestartPolicy(),entity2.getRestartPolicy())|| configEqual(entity1.getVolumes(),entity2.getVolumes());
}
