{
  String property=System.getProperty(key);
  if (property != null) {
    return property;
  }
  Object value=registry.get(key);
  if (value == null) {
    return null;
  }
  return value.toString();
}
