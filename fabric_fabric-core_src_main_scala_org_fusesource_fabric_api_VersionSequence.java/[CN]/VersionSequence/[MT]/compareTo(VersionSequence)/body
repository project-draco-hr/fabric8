{
  if (equals(that))   return 0;
  int[] n1=this.numbers;
  int[] n2=that.numbers;
  for (int i=0; i < n1.length; i++) {
    if (i >= n2.length) {
      return 1;
    }
 else {
      int diff=n1[i] - n2[i];
      if (diff != 0) {
        return diff;
      }
    }
  }
  return n1.length - n2.length;
}
