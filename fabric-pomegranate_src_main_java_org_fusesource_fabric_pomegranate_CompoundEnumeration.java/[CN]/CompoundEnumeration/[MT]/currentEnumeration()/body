{
  if (enums != null) {
    if (index < enums.length) {
      Enumeration e=enums[index];
      if (e == null || !e.hasMoreElements()) {
        e=findNextEnumeration(true);
      }
      return e;
    }
  }
  return null;
}
