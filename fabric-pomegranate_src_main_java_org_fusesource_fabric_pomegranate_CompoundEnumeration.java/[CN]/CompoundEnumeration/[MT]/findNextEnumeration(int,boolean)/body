{
  int next=cursor + 1;
  if (next < enums.length) {
    if (enums[next] != null && enums[next].hasMoreElements()) {
      if (moveCursor) {
        index=next;
      }
      return enums[next];
    }
    return findNextEnumeration(next,moveCursor);
  }
  return null;
}
