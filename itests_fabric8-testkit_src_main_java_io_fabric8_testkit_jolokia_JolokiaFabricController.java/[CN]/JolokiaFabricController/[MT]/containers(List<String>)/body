{
  List<ContainerDTO> answer=new ArrayList<>();
  for (  String id : ids) {
    ContainerDTO container=getContainer(id);
    if (container != null) {
      answer.add(container);
    }
  }
  return answer;
}
