{
  List<String> answer=new ArrayList<>();
  if (hasIds != null) {
    for (    HasId hasId : hasIds) {
      String id=hasId.getId();
      if (id != null) {
        answer.add(id);
      }
    }
  }
  return answer;
}
