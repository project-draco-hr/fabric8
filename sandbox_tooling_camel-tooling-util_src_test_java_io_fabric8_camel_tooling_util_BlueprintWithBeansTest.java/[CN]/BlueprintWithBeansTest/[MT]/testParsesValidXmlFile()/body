{
  XmlModel x=assertRoutes(new File(getBaseDir(),"src/test/resources/blueprintWithPrefix.xml"),4,CamelNamespaces.blueprintNS);
  Map<String,String> beans=x.beanMap();
  assertEquals("beans " + beans,3,beans.size());
}
