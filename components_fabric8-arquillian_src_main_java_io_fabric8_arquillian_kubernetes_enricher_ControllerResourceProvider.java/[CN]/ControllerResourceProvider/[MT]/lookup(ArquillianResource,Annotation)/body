{
  Controller controller=this.controllerInstance.get();
  if (controller == null) {
    throw new IllegalStateException("Unable to inject Kubernetes controller into test.");
  }
  return controller;
}
