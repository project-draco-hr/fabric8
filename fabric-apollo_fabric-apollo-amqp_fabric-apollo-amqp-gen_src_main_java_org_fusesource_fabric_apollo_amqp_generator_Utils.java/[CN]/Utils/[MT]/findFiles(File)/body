{
  LinkedList<File> rc=new LinkedList<File>();
  for (  File file : dir.listFiles()) {
    if (file.isFile()) {
      rc.add(file);
    }
 else {
      rc.addAll(findFiles(file));
    }
  }
  return rc;
}
