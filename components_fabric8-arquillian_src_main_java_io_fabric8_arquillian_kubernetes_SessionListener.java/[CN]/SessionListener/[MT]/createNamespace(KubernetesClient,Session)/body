{
  client.namespaces().createNew().withNewMetadata().withName(session.getNamespace()).addToLabels("provider","fabric8").addToLabels("component","integrationTest").addToLabels("framework","arquillian").withAnnotations(Util.createNamespaceAnnotations(session)).endMetadata().done();
}
