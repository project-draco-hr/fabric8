{
  writer.append('[');
  boolean first=true;
  for (  Object obj : value) {
    if (!first) {
      writer.append(',');
    }
 else {
      first=false;
    }
    write(writer,obj);
  }
  writer.append(']');
}
