{
  if (properties == true) {
    filesystem=false;
  }
  if (filesystem == true) {
    properties=false;
  }
  if (properties) {
    readPropertiesFile();
  }
  if (filesystem) {
    readFileSystem();
  }
  System.out.println("Successfully imported settings from " + source);
  return null;
}
