{
  List<String> answer=new ArrayList<String>();
  if (versions != null) {
    for (    Version version : versions) {
      String id=version.getName();
      if (id != null) {
        answer.add(id);
      }
    }
  }
  return answer;
}
