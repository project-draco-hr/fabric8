{
  PrintStream out=System.out;
  List<String> agents=service.getClusterAgents();
  if (agents != null) {
    out.println("[id]");
    for (    String agent : agents) {
      out.println(agent);
    }
  }
  return null;
}
