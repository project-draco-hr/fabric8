{
  if (sink == null) {
    C24IOContentType content=getContentType();
    if (content != null) {
      sink=createSink(content);
    }
    if (sink == null) {
      sink=createDefaultSink();
    }
  }
  return sink;
}
