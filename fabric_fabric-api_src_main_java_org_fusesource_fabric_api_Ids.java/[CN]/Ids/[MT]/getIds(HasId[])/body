{
  List<String> answer=new ArrayList<String>();
  if (hasIds != null) {
    for (    HasId hasId : hasIds) {
      String id=hasId.getId();
      if (id != null) {
        answer.add(id);
      }
    }
  }
  return answer;
}
