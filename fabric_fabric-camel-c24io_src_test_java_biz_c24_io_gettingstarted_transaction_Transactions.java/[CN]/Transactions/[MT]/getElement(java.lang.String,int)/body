{
  int i=Arrays.binarySearch(NATIVE_ELEMENTS,name);
  if (i < 0) {
    name=getSubstitute(name);
    i=Arrays.binarySearch(NATIVE_ELEMENTS,name);
  }
switch (i) {
case 1:
    return this.header;
case 0:
  if (this.customerDetails == null)   throw new java.lang.ArrayIndexOutOfBoundsException();
 else   return this.customerDetails[index];
case 2:
return this.rowCount;
default :
return super.getElement(name,index);
}
}
