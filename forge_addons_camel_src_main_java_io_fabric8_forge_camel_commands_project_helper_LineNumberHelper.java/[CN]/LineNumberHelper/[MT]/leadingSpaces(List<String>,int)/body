{
  int spaces=0;
  for (int i=0; i < index; i++) {
    String line=lines.get(i);
    if (!line.isEmpty()) {
      spaces=0;
      while (line.charAt(spaces) == ' ' && spaces < line.length()) {
        spaces++;
      }
    }
  }
  return spaces;
}
