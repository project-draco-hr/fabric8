{
  Source answer=null;
  if (exchange != null) {
    answer=exchange.getProperty("c24io.source",Source.class);
  }
  if (answer == null) {
    DataModel model=element.getModel();
    answer=model.source();
  }
  return answer;
}
