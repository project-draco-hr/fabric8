{
  int size=(int)size();
  if (size == 0) {
    return new Buffer(0);
  }
  Buffer buf=new Buffer(size);
  DataByteArrayOutputStream out=new DataByteArrayOutputStream(size);
  write(out);
  return out.toBuffer();
}
