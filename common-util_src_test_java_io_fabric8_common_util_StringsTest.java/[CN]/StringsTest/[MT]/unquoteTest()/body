{
  Assert.assertEquals("foobar",Strings.unquote("\"foobar\""));
  Assert.assertEquals("\"foobar\"",Strings.unquote("\"\"foobar\"\""));
  Assert.assertEquals("'foobar'",Strings.unquote("'foobar'"));
  Assert.assertNull(Strings.unquote(null));
}
