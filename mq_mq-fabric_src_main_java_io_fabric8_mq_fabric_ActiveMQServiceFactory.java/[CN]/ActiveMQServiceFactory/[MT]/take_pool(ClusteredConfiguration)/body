{
  if (cc.pool == null) {
    return true;
  }
 else {
    if (ownedPools.contains(cc.pool)) {
      return false;
    }
 else {
      ownedPools.add(cc.pool);
      fire_pool_change(cc);
      return true;
    }
  }
}
