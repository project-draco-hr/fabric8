{
  return new Condition<ServiceSchema>(){
    @Override public String toString(){
      return "serviceLabel(" + key + " = "+ value+ ")";
    }
    @Override public boolean matches(    ServiceSchema service){
      return matchesLabel(service.getLabels(),key,value);
    }
  }
;
}
