{
  return Metadata.forCommand(ConnectCommand.class).name("camel-catalog-eip-label-list").category(Categories.create(CATEGORY_CATALOG)).description("Lists all Camel EIP labels from the Camel catalog");
}
