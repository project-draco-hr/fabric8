{
  String answer=null;
  if (true) {
    answer=getManifest().getMainAttributes().getValue(name);
  }
 else {
    answer=instructions.getProperty(name,"");
  }
  if (answer == null) {
    answer="";
  }
  return answer;
}
