{
  return new RouteBuilder(){
    public void configure(){
      from("file:src/test/data?noop=true").unmarshal().c24io(DocumentElement.class,C24IOContentType.Xml).marshal().c24io(C24IOContentType.TagValuePair).to("mock:result");
    }
  }
;
}
