{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("timer://foo?fixedRate=true&period=5000").setBody().constant("<hello>world!</hello>").log(">>> ${body}");
    }
  }
;
}
