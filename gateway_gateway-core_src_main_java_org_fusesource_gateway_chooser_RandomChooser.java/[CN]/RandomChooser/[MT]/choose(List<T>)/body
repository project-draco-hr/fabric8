{
  int size=things.size();
  if (size == 1) {
    return things.get(0);
  }
 else   if (size > 1) {
    int idx=(int)Math.round(Math.random() * size);
    if (idx >= 0 && idx < size) {
      return things.get(idx);
    }
  }
  return null;
}
