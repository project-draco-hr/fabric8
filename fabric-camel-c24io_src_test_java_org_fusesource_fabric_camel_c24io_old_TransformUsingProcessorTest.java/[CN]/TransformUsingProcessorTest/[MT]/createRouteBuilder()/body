{
  return new RouteBuilder(){
    public void configure(){
      from("file:src/test/data?noop=true").process(C24IOSource.c24Source(Transactions.class)).process(transform(StatGenTransform.class)).to("mock:result");
    }
  }
;
}
