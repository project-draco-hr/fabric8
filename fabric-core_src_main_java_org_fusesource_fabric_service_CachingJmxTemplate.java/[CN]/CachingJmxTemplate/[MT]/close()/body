{
  if (connector != null) {
    try {
      connector.close();
    }
 catch (    IOException e) {
      throw new FabricException("Failed to close connection on agent " + agent + ". "+ e,e);
    }
 finally {
      connector=null;
    }
  }
}
