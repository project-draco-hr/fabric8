{
  return Metadata.forCommand(ConnectCommand.class).name("project-camel-component-list").category(Categories.create(CATEGORY)).description("Lists all the components the current project includes");
}
