{
  List<Entity> entities=new ArrayList<>();
  if (config != null) {
    List<Item> items=config.getItems();
    if (items != null) {
      for (      Item item : items) {
        Entity entity=getEntity(item);
        if (entity != null) {
          entities.add(entity);
        }
      }
    }
  }
  return entities;
}
