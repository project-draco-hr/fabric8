{
  if (singleton == null) {
    if (BuildSimulator.isEnabled()) {
      singleton=new SimulatorBuildTrigger();
    }
 else {
      singleton=new DefaultBuildTrigger();
    }
  }
  return singleton;
}
