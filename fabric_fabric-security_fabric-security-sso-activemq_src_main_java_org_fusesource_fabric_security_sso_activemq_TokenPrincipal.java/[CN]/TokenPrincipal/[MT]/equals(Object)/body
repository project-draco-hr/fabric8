{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  final TokenPrincipal that=(TokenPrincipal)o;
  if (!token.equals(that.token)) {
    return false;
  }
  return true;
}
