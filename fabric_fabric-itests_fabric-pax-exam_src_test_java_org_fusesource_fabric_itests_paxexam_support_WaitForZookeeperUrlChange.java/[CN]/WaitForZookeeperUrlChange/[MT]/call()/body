{
  while (keepRunning) {
    if (!url.equals(zookeeper.getConnectString())) {
      return zookeeper.getConnectString();
    }
 else {
      try {
        Thread.sleep(1000L);
      }
 catch (      Exception ex) {
      }
    }
  }
  return null;
}
