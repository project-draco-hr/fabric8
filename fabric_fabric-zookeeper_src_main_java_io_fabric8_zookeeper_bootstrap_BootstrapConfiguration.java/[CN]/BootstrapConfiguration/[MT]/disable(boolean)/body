{
  componentContext.disableComponent(COMPONENT_NAME);
  if (!async) {
    try {
      if (!deactivateLatch.await(30,TimeUnit.SECONDS))       throw new TimeoutException("Timeout for deactivating BootstrapConfiguration service");
    }
 catch (    InterruptedException ex) {
    }
  }
}
