{
  List<Pod> pods=KubernetesHelper.getPodsForReplicationController(kubernetes.replicationControllers().inNamespace(namespace).withName(replicationControllerId).get(),kubernetes.pods().inNamespace(namespace).list().getItems());
  return clientsForPod(pods);
}
