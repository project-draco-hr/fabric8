{
  List<Pod> pods=KubernetesHelper.getPodsForService(kubernetes.services().inNamespace(namespace).withName(serviceId).get(),kubernetes.pods().inNamespace(namespace).list().getItems());
  return clientForPod(pods);
}
