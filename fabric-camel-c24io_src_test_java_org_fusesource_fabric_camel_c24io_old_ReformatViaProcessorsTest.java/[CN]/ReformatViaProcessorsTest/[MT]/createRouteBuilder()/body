{
  return new RouteBuilder(){
    public void configure(){
      from("file:src/test/data?noop=true").process(C24IOSource.c24Source(Transactions.class)).process(C24IOSink.c24Sink().tagValuePair()).to("mock:result");
    }
  }
;
}
