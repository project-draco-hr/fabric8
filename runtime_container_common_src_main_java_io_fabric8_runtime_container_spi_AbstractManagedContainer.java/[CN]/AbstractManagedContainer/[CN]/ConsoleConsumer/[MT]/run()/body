{
  final InputStream stream=process.getInputStream();
  try {
    byte[] buf=new byte[32];
    int num;
    while ((num=stream.read(buf)) != -1) {
      if (config.isOutputToConsole())       System.out.write(buf,0,num);
    }
  }
 catch (  IOException e) {
  }
}
