{
  boolean foundIt=false;
  for (  String token : tokens) {
    String[] keys=token.split("=");
    for (    String key : keys) {
      if (foundIt) {
        return key;
      }
      if (typeName.equals(key)) {
        foundIt=true;
      }
    }
  }
  return null;
}
