{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  GetDataFromTreeOperation that=(GetDataFromTreeOperation)o;
  if (!fullPath.equals(that.fullPath)) {
    return false;
  }
  return true;
}
