{
  return Metadata.forCommand(ConnectCommand.class).name("camel-catalog-language-label-list").category(Categories.create(CATEGORY_CATALOG)).description("Lists all Camel language labels from the Camel catalog");
}
