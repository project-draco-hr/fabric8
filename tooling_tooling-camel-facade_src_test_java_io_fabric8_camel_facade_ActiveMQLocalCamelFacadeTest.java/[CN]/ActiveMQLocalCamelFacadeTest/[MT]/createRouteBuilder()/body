{
  return new RouteBuilder(){
    @Override public void configure() throws Exception {
      from("direct:start").routeId("in-route").to("log:in").id("toLog").to("activemq:queue:out").id("toOut");
    }
  }
;
}
