{
  validateNamespace(namespace,name);
  return handle404ByReturningNull(new Callable<BuildConfig>(){
    @Override public BuildConfig call() throws Exception {
      return getKubernetesExtensions().getBuildConfig(name,namespace);
    }
  }
);
}
