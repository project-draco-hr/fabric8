{
  validateNamespace(namespace,serviceId);
  return handle404ByReturningNull(new Callable<Service>(){
    @Override public Service call() throws Exception {
      return getKubernetes().getService(serviceId,namespace);
    }
  }
);
}
