{
  ReplicationControllerList currentReplicationControllerList=getReplicationControllers(namespace);
  return watchReplicationControllers(namespace,labels,watcher,currentReplicationControllerList.getMetadata().getResourceVersion());
}
