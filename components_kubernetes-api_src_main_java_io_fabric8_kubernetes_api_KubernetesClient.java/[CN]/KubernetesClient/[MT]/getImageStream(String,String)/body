{
  validateNamespace(namespace,name);
  return handle404ByReturningNull(new Callable<ImageStream>(){
    @Override public ImageStream call() throws Exception {
      return getKubernetesExtensions().getImageStream(name,namespace);
    }
  }
);
}
