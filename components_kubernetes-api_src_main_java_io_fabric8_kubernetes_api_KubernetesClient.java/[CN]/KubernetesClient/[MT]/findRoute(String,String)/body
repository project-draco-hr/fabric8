{
  Route route=null;
  try {
    route=getRoute(id,namespace);
  }
 catch (  WebApplicationException e) {
    if (e.getResponse().getStatus() == 404) {
    }
 else {
      throw e;
    }
  }
  return route;
}
