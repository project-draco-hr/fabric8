{
  validateNamespace(namespace,name);
  return handle404ByReturningNull(new Callable<Route>(){
    @Override public Route call() throws Exception {
      return getKubernetesExtensions().getRoute(name,namespace);
    }
  }
);
}
