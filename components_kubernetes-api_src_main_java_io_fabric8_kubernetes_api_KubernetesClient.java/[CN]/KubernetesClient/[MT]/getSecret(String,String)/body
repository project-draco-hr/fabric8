{
  validateNamespace(namespace,secretId);
  return handle404ByReturningNull(new Callable<Secret>(){
    @Override public Secret call() throws Exception {
      return getKubernetes().getSecret(secretId,namespace);
    }
  }
);
}
