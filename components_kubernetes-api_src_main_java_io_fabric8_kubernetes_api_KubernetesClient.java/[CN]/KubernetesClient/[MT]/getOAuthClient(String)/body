{
  return handle404ByReturningNull(new Callable<OAuthClient>(){
    @Override public OAuthClient call() throws Exception {
      return getKubernetesGlobalExtensions().getOAuthClient(name);
    }
  }
);
}
