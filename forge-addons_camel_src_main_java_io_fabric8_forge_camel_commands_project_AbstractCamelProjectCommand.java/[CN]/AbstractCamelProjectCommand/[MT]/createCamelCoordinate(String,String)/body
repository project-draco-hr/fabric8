{
  CoordinateBuilder builder=CoordinateBuilder.create().setGroupId("org.apache.camel").setArtifactId(artifactId);
  if (version != null) {
    builder=builder.setVersion(version);
  }
  return builder;
}
