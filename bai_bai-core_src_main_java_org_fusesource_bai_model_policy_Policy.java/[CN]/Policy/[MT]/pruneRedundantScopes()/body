{
  ScopeSet toRemove=new ScopeSet();
  for (  Scope s : scope) {
switch (s.filterElement) {
case BUNDLE:
case CONTEXT:
      if (s.expression.matches("\\**"))       toRemove.add(s);
    break;
case EVENT:
case EXCHANGE:
  break;
}
}
}
