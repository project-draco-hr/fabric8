{
  if (loginSucceeded) {
    if (debug) {
      LOG.info("Successfully logged in user with token {}",token);
    }
  }
 else {
    clear();
    if (debug) {
      LOG.info("Cleaned up failed attempt to log in");
    }
  }
  return true;
}
