{
  return Metadata.forCommand(CamelAddRouteBuilderCommand.class).name("Camel: New XML blueprint").category(Categories.create(CATEGORY)).description("Adds a new Blueprint XML file with CamelContext to your project");
}
